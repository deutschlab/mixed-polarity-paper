name: fafb783-synapse-polarity
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - xgboost
  - networkx
  - matplotlib
  - seaborn
  - pyarrow
  - joblib
  - pip
  - pip:
    - navis>=1.4
# Pin exact versions after a successful pipeline run:
#   conda env export > environment_pinned.yml
